version: "3.9"

services:
  backend:
    image: your-dockerhub-username/aesthetic-notes-backend:latest
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: production
      MONGODB_URI: ${MONGODB_URI}
      JWT_SECRET: ${JWT_SECRET}
    restart: always